# Compiler files
cache/
out/

# Commonly ignored directories
.logs/
.ignore/

# Broadcast logs — the Shannon deploy went through `forge create` + `cast`, not this script,
# so the only Deploy.s.sol runs here are the failed OOG attempts. Addresses live in the README.
/broadcast/

# Forge-generated docs
docs/

# forge verify / deploy scratch output
shannon-deployment.json

# Dotenv file
.env
.env.*
!.env.example
